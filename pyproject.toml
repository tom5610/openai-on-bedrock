[project]
name = "openai-on-bedrock"
version = "0.1.0"
description = "Run OpenAI GPT-5.5 / GPT-5.4 inference on Amazon Bedrock via the OpenAI SDK"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aws-bedrock-token-generator>=1.1.0",
    "boto3>=1.43.34",
    "openai>=2.40.0",
]
